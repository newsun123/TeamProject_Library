<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.MainMapper">
	<select id="gongji" resultType="com.example.demo.vo.GongjiVo">
		select * from gongji order by no desc limit 4
	</select>

	<select id="checkSeatNomal" resultType="int">
		select count(*) from tablename where ${time}=1 and tname like 'n%'
	</select>

	<select id="checkSeatPc" resultType="int">
		select count(*) from tablename where ${time}=1 and tname like 'pc%'
	</select>
	
	<select id="bookList" resultType="com.example.demo.vo.BookregiVo">
		select * from bookregi group by substr(bcode,1,4)
		order by no desc limit 6
	</select>
	<select id="bestBookList" resultType="com.example.demo.vo.BookregiVo">
		select * from bookregi group by substr(bcode,1,4)
		order by cnt desc limit 6
	</select>
 
 	<select id="list2" resultType="com.example.demo.vo.BookregiVo">
	<!-- type은 다쓸거기때문에 받아오는 값도 없으니 매개변수로 받은거 지우고
		 title,publi,writer로 써준다. -->
		select * from bookregi where title like concat('%',"${keyword}",'%') 
		or writer like concat('%',"${keyword}",'%') or
		publi like concat('%',"${keyword}",'%')  
		group by substr(bcode,1,4) order by ${str} limit #{start},10;
	</select>
</mapper>