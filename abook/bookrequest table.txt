create table bookrequest(
no int auto_increment primary key,
title varchar(20),
writer varchar(20),
writeyear date,
publi varchar(20),
gonge int default 0,
ect text,
userid varchar(20),
writeday date,
state int default 0);